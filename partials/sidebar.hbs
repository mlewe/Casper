{{!-- The big featured header --}}
<header class="main-header {{#if @blog.cover_image}}" style="background-image: url({{img_url @blog.cover_image}}){{else}}no-cover{{/if}}">
    <nav class="main-nav overlay clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
    </nav>
    <div class="vertical">
        <div class="main-header-content inner">
            <h1 class="page-title">{{@blog.title}}</h1>
            <h2 class="page-description">{{@blog.description}}</h2>
(I keep it dark to prevent migraines, but you can find my content in good old black-on-white on <a href="https://medium.com/@Turtle_Things/">Medium</a>.)
                        <section class="post-content">
                            </section>
        </div>
    </div>
    <a class="scroll-down icon-arrow-left" href="#content" data-offset="-45"><span class="hidden">Scroll Down</span></a>
</header>
